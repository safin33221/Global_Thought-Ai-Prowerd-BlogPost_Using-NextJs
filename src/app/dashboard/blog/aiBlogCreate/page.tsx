import React from 'react';
import BlogEditor from '../../components/BlogEditor';

const page = () => {
    return (
        <div>
            <BlogEditor />
        </div>
    );
};

export default page;